[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "manim-particles"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["manim>=0.18"]

[tool.setuptools]
py-modules = ["manim_particles"]

[tool.ruff]
line-length = 100